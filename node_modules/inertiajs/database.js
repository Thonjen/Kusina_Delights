/*link all database here*/
var mysql = require("mysql");

try{
	
	var connection = mysql.createConnection({
	  host     : 'localhost',
	  user     : 'root',
	  password : 'usbw',
	  database : 'lbdb',
	  multipleStatements: true
	});

	connection.connect();
	
	console.log("MySQL is now running");
	
}catch(e){
	
	console.log("MYSQL error : " + e);
	
}

module.exports = connection;
